# LanShare Docker环境变量配置示例
# 复制此文件为 .env 并根据你的网络环境修改

# 服务端口
PORT=7070

# 运行环境
# development/production
FLASK_ENV=production

# 宿主机的局域网IP地址
# 在Windows上可以通过命令行运行: ipconfig 查看
# 在macOS/Linux上可以通过命令行运行: ifconfig 或 ip addr 查看
# 例如: 192.168.1.100
HOST_IP=192.168.1.100

# Docker宿主机的局域网IP（可选，与HOST_IP作用相同）
DOCKER_HOST_IP=192.168.1.100

# 上传目录
UPLOAD_FOLDER=./uploads

# 数据库路径
DATABASE_PATH=./data/lanshare.db

# 密钥
SECRET_KEY=lanshare-secret-key-2025

# 自动清理天数
AUTO_CLEAN_DAYS=7